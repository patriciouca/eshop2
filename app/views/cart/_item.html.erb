<%= link_to item.movie.title, :action => 'show', :controller => 'catalog', :id => item.movie.id %>

<%= pluralize item.amount, "movie", "movies" %>, <%= sprintf "%0.2f €", item.price * item.amount %>

(<%= link_to '<b>-</b>'.html_safe, :controller => 'cart', :action => 'remove', :id => item.movie %>)
